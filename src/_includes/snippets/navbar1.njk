<nav id="navScroll" class="navbar navbar-expand-lg navbar-light white-text container {% if stickyNav %}fixed-top{% endif %} py-lg-3 pb-0 px-vw-6" tabindex="0">
  <div class="container site-navbar__inner d-flex flex-column d-lg-flex flex-lg-row justify-content-between align-items-center w-100">

    {# Show "DUST WAVE" link on mobile, but hide it on homepage #}
    {% if not isHomepage %}
    <div class="d-lg-none w-100 text-center mb-2">
      <h1 class="display-huge mt-3 mb-3">
        <a href="{{ i18n | localizedUrl(language, 'home', '/') }}" class="text-white text-decoration-none" aria-label="{{ i18n | t(language, 'nav.homeAria') }}">DUST WAVE</a>
      </h1>
    </div>
    {% endif %}

    <div class="site-navbar__brand d-none d-lg-block">
      {% include "snippets/navbar-brand.njk" %}
    </div>

    <button class="navbar-toggler" type="button" data-site-nav-toggle
      aria-controls="navbarContent" aria-expanded="false" aria-label="{{ i18n | t(language, 'nav.toggle') }}">
      <span class="navbar-toggler-icon"></span>
    </button>

    {% include "snippets/navbar-content.njk" %}
  </div>
</nav>
