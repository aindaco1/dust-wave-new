<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="robots" content="noindex, nofollow">
  <title>Substack Export: {{ title }}</title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      max-width: 680px;
      margin: 0 auto;
      padding: 20px;
      background: #fafafa;
      line-height: 1.6;
    }
    .toolbar {
      background: #1a1a2e;
      color: white;
      padding: 15px 20px;
      border-radius: 8px;
      margin-bottom: 20px;
    }
    .toolbar h1 { margin: 0 0 10px 0; font-size: 16px; }
    .toolbar p { margin: 0 0 15px 0; font-size: 13px; opacity: 0.8; }
    .toolbar button {
      background: #4a9eff;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 600;
    }
    .toolbar button:hover { background: #3a8eef; }
    .toolbar .success { color: #4ade80; margin-left: 10px; display: none; }
    .content-box {
      background: white;
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 30px;
    }
    .content-box h1 { margin-top: 0; }
    .content-box img { max-width: 100%; height: auto; }
    .back-link { margin-top: 20px; text-align: center; }
    .back-link a { color: #666; }
  </style>
</head>
<body>
  <div class="toolbar">
    <h1>📋 Substack Export</h1>
    <p>This is a clean version of "{{ title }}" optimized for pasting into Substack's editor.</p>
    <button onclick="copyContent()">Copy to Clipboard</button>
    <span class="success" id="success">✓ Copied!</span>
  </div>

  <div class="content-box" id="substack-content">
    <h1>{{ title }}</h1>
    
    {% if img %}
    <img src="{{ metadata.url }}{{ img }}" alt="{{ imgAlt or title }}">
    {% endif %}

    {{ content | substackClean | safe }}

    <p><em>Originally published at <a href="{{ metadata.url }}{{ page.url | replace('/substack-export/', '/') | replace('.html', '') }}.html">{{ metadata.url }}</a></em></p>
  </div>

  <div class="back-link">
    <a href="{{ page.url | replace('/substack-export/', '/') | replace('.html', '') }}.html">← Back to original article</a>
  </div>

  <script>
    function copyContent() {
      const content = document.getElementById('substack-content');
      const range = document.createRange();
      range.selectNodeContents(content);
      const selection = window.getSelection();
      selection.removeAllRanges();
      selection.addRange(range);
      document.execCommand('copy');
      selection.removeAllRanges();
      document.getElementById('success').style.display = 'inline';
      setTimeout(() => {
        document.getElementById('success').style.display = 'none';
      }, 2000);
    }
  </script>
</body>
</html>
