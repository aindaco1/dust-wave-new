media:
  input: src/img
  output: img
  path: src/img

content:
  # ═══════════════════════════════════════════════════════════
  # 🎬 POSTS — Film projects and productions
  # ═══════════════════════════════════════════════════════════
  - name: posts
    label: 🎬 Posts (Film Projects)
    type: collection
    path: src/posts
    filename: "{fields.title}.md"
    view:
      fields: [title, date, tags]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Title
        type: string
        description: "The project title (e.g., 'Stall'd', 'The Haunters')."

      - name: date
        label: Date
        type: date
        options:
          format: yyyy-mm-dd
        description: "Release or premiere date. Format: YYYY-MM-DD."

      - name: img
        label: Featured Image
        type: image
        description: |
          Main image shown in listings. Best size: 1200x675px (16:9).
          Use a still from the film or promo image. PNG or JPG.

      - name: summary
        label: Short Description
        type: string
        options:
          maxlength: 160
        description: "One-line description shown in listings (max 160 chars). E.g., 'Romance drama short'."

      - name: gif
        label: Hover GIF
        type: image
        options:
          extensions: [gif]
        description: |
          Animated GIF shown on homepage hover effect.
          Path should start with /img/ (e.g., /img/stalld.gif).
          Optimal size: 480x270px, under 2MB.

      - name: tags
        label: Tags
        type: string
        list: true
        description: |
          Categories for this project. Common tags:
          • Genre: drama, comedy, horror, documentary
          • Status: released, in-production, coming-soon
          Add one tag per line.

      - name: body
        label: Content
        type: rich-text
        description: |
          Full project description with crew, cast, awards, etc.
          Use headings (### Crew, ### Cast) to organize sections.
          Images can be added using the image button.

  # ═══════════════════════════════════════════════════════════
  # 📰 NEWS — Blog posts and digests
  # ═══════════════════════════════════════════════════════════
  - name: news
    label: 📰 News & Digests
    type: collection
    path: src/news
    filename: "{fields.title}.md"
    view:
      fields: [title, date]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Title
        type: string
        description: "Article headline (e.g., 'DIY Filmmaker Digest -- Week of December 8, 2025')."

      - name: date
        label: Date
        type: date
        options:
          format: yyyy-mm-dd
        description: "Publication date. Format: YYYY-MM-DD."

      - name: summary
        label: Short Description
        type: string
        options:
          maxlength: 160
        description: "Preview text shown in listings and social shares (max 160 chars)."

      - name: img
        label: Header Image
        type: image
        description: |
          Image shown at the top of the article.
          Best size: 1200x630px. PNG or JPG.

      - name: body
        label: Article Content
        type: rich-text
        description: |
          The main article content. For Digest posts, this contains the
          curated links, podcasts, and videos in a specific HTML format.
          
          ⚠️ DIGEST POSTS: These are generated by a Python script.
          Manual edits are okay, but the structure should be preserved.

  # ═══════════════════════════════════════════════════════════
  # 📅 EVENTS — Screenings and festivals
  # ═══════════════════════════════════════════════════════════
  - name: events
    label: 📅 Events (Screenings)
    type: collection
    path: src/events
    filename: "{fields.title}.md"
    view:
      fields: [title, date, summary]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Event Title
        type: string
        description: "Full event name (e.g., 'Fronteras Micro-film Festival at MOOD')."

      - name: date
        label: Event Date
        type: date
        options:
          format: yyyy-mm-dd
        description: "When the event takes place. Format: YYYY-MM-DD."

      - name: layout
        label: Layout Template
        type: string
        default: layouts/event.njk
        hidden: true

      - name: permalink
        label: URL Slug
        type: string
        description: |
          The page URL (e.g., 'fronteras-at-mood.html').
          Use lowercase with dashes, end with .html.

      - name: summary
        label: Short Description
        type: string
        description: "One-paragraph event description for listings and social shares."

      - name: img
        label: Event Image
        type: image
        description: |
          Promotional image for the event.
          Best size: 1200x675px (16:9). PNG or JPG.

      - name: body
        label: Event Details
        type: rich-text
        description: |
          Full event description including:
          • Date, time, and venue
          • Ticket links
          • Program details
          • Images and flyers
