media:
  input: src/img
  output: /img

content:
  # ═══════════════════════════════════════════════════════════
  # 🎬 POSTS — Film projects and productions
  # ═══════════════════════════════════════════════════════════
  - name: posts
    label: 🎬 Film Projects
    type: collection
    path: src/posts
    view:
      fields: [title, date, tags]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Title
        type: string
        description: "The project title (e.g., 'Stall'd', 'The Haunters')."

      - name: date
        label: Release Date
        type: date
        description: "Release or premiere date."

      - name: img
        label: Featured Image
        type: image
        description: |
          Main image shown on the project page.
          Best size: 1200x675px (16:9). JPG or PNG.

      - name: summary
        label: Short Description
        type: string
        description: "One-line tagline (e.g., 'Romance drama short', 'Dark horror comedy')."

      - name: gif
        label: Hover GIF
        type: image
        options:
          extensions: [gif]
        description: |
          Animated GIF for homepage hover effect.
          Optimal size: 480x270px, under 2MB.

      - name: tags
        label: Tags
        type: select
        list: true
        options:
          values:
            - value: released
              label: "✅ Released"
            - value: in-production
              label: "🎬 In Production"
            - value: coming-soon
              label: "🔜 Coming Soon"
            - value: comedy
              label: "Comedy"
            - value: drama
              label: "Drama"
            - value: horror
              label: "Horror"
            - value: sci-fi
              label: "Sci-Fi"
            - value: documentary
              label: "Documentary"
            - value: satire
              label: "Satire"
            - value: mockumentary
              label: "Mockumentary"
            - value: stop-motion
              label: "Stop-Motion"
            - value: silent
              label: "Silent"
            - value: musical
              label: "Musical"
            - value: noir
              label: "Noir"
            - value: romance
              label: "Romance"
            - value: festival
              label: "Festival/Event"
            - value: event
              label: "Event"
        description: "Select all that apply. Include status + genre(s)."

      - name: videoId
        label: Video ID
        type: string
        description: |
          YouTube or Vimeo video ID for the movie embed.
          YouTube: the part after "v=" (e.g., 5JyyQVF0cAI)
          Vimeo: the number at the end of the URL

      - name: videoType
        label: Video Platform
        type: select
        options:
          values:
            - value: youtube
              label: YouTube
            - value: vimeo
              label: Vimeo
        description: "Select the video platform."

      - name: galleryImages
        label: 📸 Behind the Scenes Gallery
        type: image
        list: true
        description: |
          Upload images for the gallery section.
          These will automatically display in a 2-column grid.
          Best size: 800x600px or similar. JPG or PNG.

      - name: poster
        label: 🎨 Poster Image
        type: image
        description: |
          Optional poster image displayed after the gallery.
          Best size: 600x900px (2:3 ratio).

      - name: body
        label: Content
        type: rich-text
        default: |
          _TITLE_ is a SHORT DESCRIPTION created for OCCASION. Shot at LOCATION in Albuquerque, New Mexico.
          <br><br>

          ### Crew

          Directed by NAME<br>
          Written by NAME<br>
          Produced by NAME<br>
          <br>
          Director of Photography -- NAME<br>
          Editor -- NAME<br>
          Sound -- NAME<br>
          <br><br>

          ### Cast

          CHARACTER -- ACTOR<br>
          CHARACTER -- ACTOR<br>
          <br><br>

          ### Awards

          [FESTIVAL NAME](https://example.com)
          * Award Name -- Recipient
          * **Best Film**
        description: |
          Project description, crew, cast, and awards.
          Replace the CAPS placeholders with your info.
          
          📸 **Gallery & video are added via the fields above** — no HTML needed!
          
          For inline images, use: `{% img "/img/photo.jpg", "description" %}`

  # ═══════════════════════════════════════════════════════════
  # 📜 DIY DIGESTS — Auto-generated weekly digests (code editing)
  # ═══════════════════════════════════════════════════════════
  - name: digests
    label: 📜 DIY Digests (⚠️ Edit Only - Do Not Create)
    type: collection
    path: src/news/digests
    view:
      fields: [title, date]
      primary: title
      sort: [date]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Title
        type: string
        description: "Digest title (e.g., 'DIY Filmmaker Digest 8 -- Week of December 8, 2025')."

      - name: date
        label: Date
        type: date
        description: "Publication date."

      - name: summary
        label: Short Description
        type: string
        description: "One-sentence preview for listings."

      - name: img
        label: Header Image
        type: image
        description: "Header image for the digest."

      - name: body
        label: Digest Content (HTML)
        type: code
        options:
          lang: html
        description: |
          ⚠️ **This is raw HTML** — edit carefully!
          
          Digest posts are auto-generated by scripts.
          Only make small text edits, don't restructure the HTML.

  # ═══════════════════════════════════════════════════════════
  # 📢 REGULAR NEWS — Standard announcements (rich-text editing)
  # ═══════════════════════════════════════════════════════════
  - name: news-regular
    label: 📢 News (Regular)
    type: collection
    path: src/news
    subfolders: false
    view:
      fields: [title, date]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Title
        type: string
        description: "Article headline (e.g., 'Bag Check selected at SISFA!')."

      - name: date
        label: Date
        type: date
        description: "Publication date."

      - name: summary
        label: Short Description
        type: string
        description: "One-sentence preview for listings (max 160 chars)."

      - name: img
        label: Header Image
        type: image
        description: |
          Image shown at the top of the article.
          Best size: 1200x630px. JPG or PNG.

      - name: galleryImages
        label: 📸 Article Gallery
        type: image
        list: true
        description: |
          Upload images for an auto-generated gallery.
          These display in a 2-column grid at the end of the article.

      - name: body
        label: Article Content
        type: rich-text
        default: |
          <br>

          Congratulations to everyone involved in PROJECT NAME!

          <br>

          WRITE YOUR ANNOUNCEMENT HERE. Include links to relevant projects like [Project Name](/subdir/project-name/).
        description: |
          The article body. Replace CAPS placeholders.
          
          📸 **Gallery images are added via the field above** — no HTML needed!
          
          ⚠️ DIY DIGEST posts are auto-generated — edit with care!
          
          For inline images, use: `{% img "/img/news/photo.jpg", "description" %}`
          For videos, use: `{% youtube "VIDEO_ID" %}`

  # ═══════════════════════════════════════════════════════════
  # 📅 EVENTS — Screenings and festivals
  # ═══════════════════════════════════════════════════════════
  - name: events
    label: 📅 Events & Screenings
    type: collection
    path: src/events
    view:
      fields: [title, date, summary]
      primary: title
      sort: [date, title]
      default:
        sort: date
        order: desc
    fields:
      - name: title
        label: Event Title
        type: string
        description: "Full event name (e.g., 'Fronteras Micro-film Festival at MOOD')."

      - name: date
        label: Event Date
        type: date
        description: "The date of the event."

      - name: layout
        label: Layout Template
        type: string
        default: layouts/event.njk
        hidden: true

      - name: permalink
        label: URL Slug
        type: string
        description: |
          The page URL (e.g., 'fronteras-at-mood.html').
          Lowercase with dashes, must end with .html

      - name: summary
        label: Short Description
        type: string
        description: "One-paragraph event description for listings."

      - name: img
        label: Event Image
        type: image
        description: |
          Promotional image for the event.
          Best size: 1200x675px (16:9). JPG or PNG.

      - name: galleryImages
        label: 📸 Event Gallery
        type: image
        list: true
        description: |
          Upload flyers, photos, etc. for an auto-generated gallery.
          These display in a 2-column grid.

      - name: body
        label: Event Details
        type: rich-text
        default: |
          <br>

          <h4>DATE (e.g., March 1, 2024)</h4>
          <h4>VENUE, Albuquerque, New Mexico</h4>
          <h4>TIME (e.g., 7:00 PM - 10:00 PM)</h4>

          <br>

          EVENT DESCRIPTION GOES HERE. Include ticket links, program details, etc.

          <br>

          More info at [EXTERNAL LINK](https://example.com)
        description: |
          Event details including date, time, venue, and images.
          Replace the CAPS placeholders with your info.
          
          📸 **Gallery images are added via the field above** — no HTML needed!
          
          For inline images, use: `{% img "/img/events/photo.jpg", "description" %}`
