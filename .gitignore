dev/
node_modules/
.DS_Store
.cache
.wrangler/
.artifacts/
.dev.vars
.env
.env.*

# docs/ is generated by GitHub Actions - don't commit locally
# This prevents merge conflicts between local builds and CI builds
docs/

# Legacy Eleventy output at the repository root (source lives in src/)
/project/

# WebP derivatives are generated only in the GitHub Pages artifact
src/img/webp/
